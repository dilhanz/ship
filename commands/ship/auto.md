---
description: Capture requirements interactively, confirm a roadmap, then automatically plan, execute, and verify all phases. Stops and reports if a blocker is hit.
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, WebFetch
---

Follow the auto workflow at `.claude/ship/workflows/auto.md`.

$ARGUMENTS
